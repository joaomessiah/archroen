# Output schema: the pottery summary CSV

The workflow's one deliverable is a CSV per report at `output_files/reports/<folder>/<report>.csv`.
**Each row is one distinct pottery find** that the report reports as present, after deduplication,
consolidation, and the Roman-period scope filter. Open it in any spreadsheet program.

The columns, in order:

| Column | Meaning |
|---|---|
| `report_id` | The report's identifier (its PDF filename stem). |
| `site_name` | The find's site, after site-name normalization (variants of one place collapsed to one label). |
| `page` | The page the find was reported on. |
| `pottery` | The pottery find's name (descriptive/canonical). |
| `typology` | The typology code if one applies (e.g. `Drag. 37`, `Dressel 20`); blank otherwise. |
| `term_found` | The term as it was actually detected in the report text. |
| `term_found_normalized_en` | That term normalized to its canonical English label. |
| `quantity` | How many were reported, when a count is stated. |
| `start_date` | Start of the date range, as a year (negative = BC). Blank if undated. |
| `end_date` | End of the date range, as a year (negative = BC). Blank if undated. |
| `date_method` | How the date was derived (a finite set of tags; see below). Blank when the find is undated. |
| `context_label` | How the report referred to the find (`present`, `comparison`, …); see [../workflow/specs/layer_5.md](../workflow/specs/layer_5.md). |
| `pot_name_certainty_level` | Confidence in the pottery name, an integer 0-10 (10 = highest). |
| `pot_name_llm_reasoning` | The AI's explanation for the name, when the AI was involved. |
| `pot_presence_certainty_level` | Confidence that the pot was actually found (vs compared/cited). |
| `pot_presence_llm_reasoning` | The AI's explanation for the presence judgment, when involved. |
| `dates_certainty_level` | Confidence in the assigned date range. |
| `date_llm_reasoning` | The AI's explanation for the date, when the AI was involved. |
| `overall_certainty_level` | A combined confidence for the whole row. |
| `original_text` | The verbatim text the find was drawn from: the evidence trail. |
| `std_vocabulary` | The standard vocabulary the `std_*` codes belong to (`ABR`), or blank if the find resolved to none. |
| `std_ware_code` | The find's ware/fabric family as a standard code (e.g. `TS`). |
| `std_ware_label` | The label for that ware code (e.g. `terra sigillata`). |
| `std_form_code` | The vessel form as a standard code (e.g. `KOM.KOM`). |
| `std_form_label` | The label for that form code (e.g. `kom`). |
| `std_combined_code` | The combined ware+form(+typology) term as a standard code (e.g. `TSKOM.DR37`). |
| `std_combined_label` | The label for that combined code (e.g. `Terra sigillata kom - Dragendorff 37`). |

## Standard-vocabulary columns (`std_*`)

The last seven columns map each find to a **standard controlled vocabulary**, currently the Dutch
**ABR** (Archeologisch Basisregister). They are produced by a deterministic, mode-independent step at
the tail of Layer 7 (`src/standard_vocab.py`), gated by `STANDARD_VOCAB_USE` /
`STANDARD_VOCAB_STYLE` in `config.py` (default on, style `abr`). When off, the columns are not emitted.

- **Standards interoperability.** These codes map each find to the Dutch national standard (ABR/Archis),
  so the output can be reused directly alongside national heritage data. They form a deterministic layer
  separate from the scored evaluation (Layer 8 scores the find list and dates, not these mappings), so
  they never affect the accuracy metrics; spot-checks consistently confirm the codes resolve correctly.
- **Standard-agnostic by design.** The target standard is chosen by `STANDARD_VOCAB_STYLE`; ABR is the
  first one implemented, and another vocabulary (e.g. a different country's) is a drop-in addition under
  `data/vocabularies/standards/<style>/`.
- **Works in every mode.** Resolution is deterministic and needs no model, so the columns are produced
  in all modes, including rules-only.
- **Labels are Dutch** (the official ABR terms); `std_vocabulary=ABR` records the source.
- **Resolution:** when ABR recognizes the typology, the ware, form, and combined term are read from its
  authoritative combiterm (preferring a region-agnostic one, else the region the text names, else the
  sole variant). Otherwise the ware and form come from the master vocab or a text fallback, and the
  combined term is filled only when that ware+form pair matches an ABR combiterm. A find that resolves
  nothing gets blank `std_*` columns, and the combined column stays blank whenever no combiterm matches.
- The maps live under `data/vocabularies/standards/abr/` and are regenerated by
  `tools/build_abr_maps.py` from a frozen ABR snapshot. See [data_files.md](data_files.md).

## `date_method` values

`date_method` is one of a finite set of tags. The rule pipeline emits:

- `typology`: from the canonical typology table (the most reliable).
- `text_explicit`: from explicit dates stated in the text.
- `period_term`: from a period/century term in the find's own quote.
- `section_phase`: from a dated phase in the section.
- `llm_context`: from dates the LLM read out of the context.
- `llm`: from the LLM's own date judgment.
- `context_clamp`: a missing endpoint filled from the Roman period context.

In the AI modes the hybrid extractor tags the backend it used (e.g. `claude`, `claude_text`,
`claude+typology`, `rules+claude`, `rules+claude_confirmed`; the prefix is the active backend). A
`+typology` suffix means the date was re-grounded against the typology table; a `_text` suffix means it
came from explicit text dates.

## Example row

A real row from `output_files/reports/south_limburg_villas/13319.csv` (Claude mode):

| Column | Value |
|---|---|
| `report_id` | `13319` |
| `site_name` | `Kerkrade-Holzkuil` |
| `page` | `216` |
| `pottery` | `Terra sigillata decorated bowl` |
| `typology` | `Dragendorff 37` |
| `term_found` | `kom van het type Dragendorff 37` |
| `term_found_normalized_en` | `Terra sigillata decorated bowl` |
| `quantity` | `1` |
| `start_date` | `70` |
| `end_date` | `300` |
| `date_method` | `claude+typology` |
| `context_label` | `present` |
| `pot_name_certainty_level` | `10` |
| `pot_presence_certainty_level` | `9` |
| `dates_certainty_level` | `10` |
| `overall_certainty_level` | `10` |
| `original_text` | `Een verbrande terra sigillata scherf behoorde tot een kom van het type Dragendorff 37.` |
| `std_vocabulary` | `ABR` |
| `std_ware_code` | `TS` |
| `std_ware_label` | `terra sigillata` |
| `std_form_code` | `KOM.KOM` |
| `std_form_label` | `kom` |
| `std_combined_code` | `TSKOM.DR37` |
| `std_combined_label` | `Terra sigillata kom - Dragendorff 37` |

(The `*_llm_reasoning` columns are omitted here for width; in this row they carry the model's short
justifications. Because Dragendorff 37 is a typology ABR recognizes, `std_combined` is its authoritative
combiterm `TSKOM.DR37` and the ware and form are read from it. A find whose typology has no ABR
combiterm instead falls back to a ware+form-level code, and one too generic to place is left blank.)

## Reading tips

- **A blank `start_date`/`end_date`** usually means the find is genuine but **undated** in the report.
  It is still kept, because the Roman scope filter keeps undated finds.
- **`date_method`** tells you *how much to trust* a date: a typology-table date is the most reliable,
  and an AI-derived date is the least.
- The **`*_llm_reasoning`** columns are only populated when an AI mode was used and the AI made that
  particular judgment. In **Rules-only mode** they are blank.
- The **certainty levels** are recorded separately for the name, the presence, the dates, and overall,
  so you can filter to the rows you trust most (see the note below on the scale).

The **certainty levels** are an integer 0-10 score (0 = none/lowest, 10 = highest).

For how these rows are produced, see [../workflow/specs/layer_7.md](../workflow/specs/layer_7.md).
